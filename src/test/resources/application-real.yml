spring:
  profiles:
    include: real-db

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB106Dialect
    hibernate:
      ddl-auto: none

  session:
    store-type: jdbc

file:
  dir: /home/ec2-user/app/file